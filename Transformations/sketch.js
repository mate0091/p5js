var angle = 1;

function setup() {
  var canvas = createCanvas(300, 600);
  canvas.parent("sketch");

  createP("Another paragraph");

  angleMode(DEGREES);
  rectMode(CENTER);
}

function draw() {
  //background(0);
  //fill(255);

  //rectangle;
}
